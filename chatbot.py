import src/onto
import src/window

# still = 1
# while still == 1:
#   mode = selectModeBox()
#   cancel, query, message = queryBox(mode)
#   answer = function(query)
#   answerBox(cancel, query, message, answer)
#   still = endBox()
