import src/onto     # pour charger l ontologie
import src/window   # pour charger l interface graphique
# import src/       # fichier de Younes
# import src/       # fichier de Romain

# still = 1
# while still == 1:
#   mode = selectModeBox()
#   cancel, query, message = queryBox(mode)
#
#   answer = function(query)  # function est tout ce qui traite la requete et
#                               renvoie la reponse
#
#   answerBox(cancel, query, message, answer)
#   still = endBox()
